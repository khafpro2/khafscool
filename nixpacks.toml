[phases.install]
cmds = ["corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @ama/shared build",
    "pnpm --filter backend exec prisma generate",
      "pnpm --filter backend build"
      ]
